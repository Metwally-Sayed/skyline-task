import React from "react";
import Layout from "../Layout";

type Props = {};

const cart = (props: Props) => {
  return (
    <Layout>
      <div>Cart</div>
    </Layout>
  );
};

export default cart;
